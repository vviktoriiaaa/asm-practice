# Практична 10. Бітові операції: маски, зсуви, підрахунок бітів

**Мова:** Assembler i386 (NASM)  
**ОС:** Debian Linux  
**Ввід/вивід:** лише `int 0x80` (sys_read/sys_write/sys_exit)

## Мета
Навчитися друкувати число в двійковому вигляді, рахувати біти та застосовувати маски.

## Вхідні дані
Одне 32-бітне число x.

## Вихідні дані
1) x у двійковому вигляді (32 біти, групи по 4)
2) popcount(x)
3) x після: set бітів p,q та clear біта r (показати як десяткове число)

## Що потрібно використати
- and/or/xor/not, shl/shr
- маски (1<<pos)
- цикл 32 ітерації для двійкового друку
- popcount через shr + and 1

## Критерії до коду
- Код компілюється NASM та лінкується `ld` (команди нижче).
- У коді є коментарі базових блоків: `I/O`, `parse`, `math`, `logic`, `loops`, `memory`.
- Немає коментарів, що згадують ChatGPT.
- Результат відповідає вимогам задачі та коректно обробляє описані граничні випадки.

## Збірка і запуск
```bash
./compile.sh
./link.sh
./practice10
```

## Доказ виконання
У `screenshot.png` має бути видно:
1) запуск `./compile.sh`
2) запуск `./link.sh`
3) запуск програми та її вивід
