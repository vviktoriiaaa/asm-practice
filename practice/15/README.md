# Практична 15. Рекурсія: factorial + лічильник викликів

**Мова:** Assembler i386 (NASM)  
**ОС:** Debian Linux  
**Ввід/вивід:** лише `int 0x80` (sys_read/sys_write/sys_exit)

## Мета
Закріпити роботу зі стеком, рекурсію, збереження регістрів та підрахунок викликів.

## Вхідні дані
n (0..12 для factorial).

## Вихідні дані
1) fact(n)
2) calls = кількість рекурсивних викликів

## Що потрібно використати
- рекурсивна функція fact
- пролог/епілог, збереження потрібних регістрів
- параметр у eax, результат у eax
- глобальний лічильник calls у .bss

## Критерії до коду
- Код компілюється NASM та лінкується `ld` (команди нижче).
- У коді є коментарі базових блоків: `I/O`, `parse`, `math`, `logic`, `loops`, `memory`.
- Немає коментарів, що згадують ChatGPT.
- Результат відповідає вимогам задачі та коректно обробляє описані граничні випадки.

## Збірка і запуск
```bash
./compile.sh
./link.sh
./practice15
```

## Доказ виконання
У `screenshot.png` має бути видно:
1) запуск `./compile.sh`
2) запуск `./link.sh`
3) запуск програми та її вивід
