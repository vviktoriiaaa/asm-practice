# Практична 6. Signed vs Unsigned: порівняння і гілкування

**Мова:** Assembler i386 (NASM)  
**ОС:** Debian Linux  
**Ввід/вивід:** лише `int 0x80` (sys_read/sys_write/sys_exit)

## Мета
Навчитися відрізняти signed/unsigned порівняння та використовувати відповідні умовні переходи.

## Вхідні дані
Два 32-бітні числа a і b (дозволено від’ємні).

## Вихідні дані
4 рядки:
1) SIGNED: a < b | a = b | a > b
2) UNSIGNED: a < b | a = b | a > b
3) max_signed(a,b)
4) max_unsigned(a,b)

## Що потрібно використати
- jl/jg/je для signed, jb/ja/je для unsigned
- акуратний форматований текст у виводі
- розділити логіку на підпрограми (наприклад cmp_signed, cmp_unsigned)

## Критерії до коду
- Код компілюється NASM та лінкується `ld` (команди нижче).
- У коді є коментарі базових блоків: `I/O`, `parse`, `math`, `logic`, `loops`, `memory`.
- Немає коментарів, що згадують ChatGPT.
- Результат відповідає вимогам задачі та коректно обробляє описані граничні випадки.

## Збірка і запуск
```bash
./compile.sh
./link.sh
./practice6
```

## Доказ виконання
У `screenshot.png` має бути видно:
1) запуск `./compile.sh`
2) запуск `./link.sh`
3) запуск програми та її вивід
