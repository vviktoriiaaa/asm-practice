# Практична 7. Масив у пам’яті: заповнення і пошук мін/макс

**Мова:** Assembler i386 (NASM)  
**ОС:** Debian Linux  
**Ввід/вивід:** лише `int 0x80` (sys_read/sys_write/sys_exit)

## Мета
Побудувати масив у .bss, заповнити його формулою, знайти min/max та їх індекси.

## Вхідні дані
n (5..50).

## Вихідні дані
1) Масив n чисел в один рядок
2) min і його індекс
3) max і його індекс

## Що потрібно використати
- масив dd 50 dup(?) у .bss
- індексація [base + idx*4]
- цикл for по індексах
- формула генерації елементів

## Критерії до коду
- Код компілюється NASM та лінкується `ld` (команди нижче).
- У коді є коментарі базових блоків: `I/O`, `parse`, `math`, `logic`, `loops`, `memory`.
- Немає коментарів, що згадують ChatGPT.
- Результат відповідає вимогам задачі та коректно обробляє описані граничні випадки.

## Збірка і запуск
```bash
./compile.sh
./link.sh
./practice7
```

## Доказ виконання
У `screenshot.png` має бути видно:
1) запуск `./compile.sh`
2) запуск `./link.sh`
3) запуск програми та її вивід
