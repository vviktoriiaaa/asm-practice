# Практична 9. Генерація псевдорандомних даних (LCG) і гістограма

**Мова:** Assembler i386 (NASM)  
**ОС:** Debian Linux  
**Ввід/вивід:** лише `int 0x80` (sys_read/sys_write/sys_exit)

## Мета
Згенерувати псевдовипадкові значення, порахувати частоти та вивести гістограму.

## Вхідні дані
n (100..1000).

## Вихідні дані
10 рядків:
0: ##### (count)
...
9: ### (count)
де кількість # пропорційна count (можна 1 # на 1 елемент, або масштабування).

## Що потрібно використати
- LCG: x = (1103515245*x + 12345) mod 2^31

- масив частот freq[10]
- цикли та інкременти

## Критерії до коду
- Код компілюється NASM та лінкується `ld` (команди нижче).
- У коді є коментарі базових блоків: `I/O`, `parse`, `math`, `logic`, `loops`, `memory`.
- Немає коментарів, що згадують ChatGPT.
- Результат відповідає вимогам задачі та коректно обробляє описані граничні випадки.

## Збірка і запуск
```bash
./compile.sh
./link.sh
./practice9
```

## Доказ виконання
У `screenshot.png` має бути видно:
1) запуск `./compile.sh`
2) запуск `./link.sh`
3) запуск програми та її вивід
